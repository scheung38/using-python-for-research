def is_vowel(letter):
    # if  # blank#:
    if letter in ('aeiouy'):
        return (True)
    else:
        return (False)

# Can you replace #blank# in the second line so
# is_vowel becomes a function that takes a letter
# as input and prints whether a letter is a vowel
# (in "aeiouy")?

print(is_vowel(4))